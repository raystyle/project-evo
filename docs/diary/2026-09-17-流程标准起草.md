# 2026-09-17:流程标准起草

> 当日主线:ohmycloud 总台派单的编译打包发布流程标准(flow-build)起草。采守各七条,追正分工核心条款,实录新增两条。

## 第七十五批:flow-build 编译打包发布流程标准

- 总台派单形态:参考件通篇读加 flow-release 边界定形(新篇 flow-build.md,封版操作面留 flow-release 互引);裁定面采 7 守 7 由总台给齐,追正「编译产地与播种分工」核心条款置第二节(总台 rclone 仅 chrome 级大件;小件仓内 Actions 全链自包零总台手工,过渡期特例禁新形态;CI 内 rclone 与总台 rclone 分表述)
- 实证锚自 ohmycloud 09-17 日记与各仓 REQ:aria2 v1.37.2 五岗绿(跨宿主容器闸收口)、ark v1.3.0 Draft 态代发尾例、hst v2.0.0 六岗、browse v0.2.0 REQ-003、REQ-053 全链闭环
- 回执:标准档路径与要点映射表(采 7 守 7 新增 2)发总台 wR:p1;随后总台广播对齐单到全仓
- 追正二落地:「自升级与 ark 升级对齐」章(第八节):self update 双通道与 digest 判新同判据(ark 加 hst 实证)、静态件升级归安装管理方(ark catalog pin 与 browse 版本管理器分工)、五端终态同 digest 双面核;排障表加升级态漂移行
- 追正三落地:自升级章精化(第八节):能力归属分族(自研仓 hst、ark、officecli、reader 恒带 self update,fork 族可不带归 ark 单通道);ark 元数据 locked、installed、path 三态为全 fleet 共同真源,升级器须回写对齐或 ark 以 installed 探活实值判;reader 双通道判新互认(五端 self update 与 ark update 同报已最新)入验收判据;排障表加元数据旧漂行
- 快核处置:F1 必修(推送形改可执行形,--s3-no-check-bucket 旗标加 Cache-Control immutable 头,族内四仓同形实证)加 F2(指针第五节)修;G1 两处补标态、G2 docs 地图对称行、G3 锚句指 flow-release 第八节,全采纳
- 追正四落地并章定稿:自升级家族补 browse 成五自研仓;browse 双职责特注(自身 CLI 双通道自升级加 chromeInstall 集成管理 chrome 大件安装);chrome 大件资源分发归 omc 接管(chrome 桶与推件归总台,AGENTS 例外条款在案)
- 定形重构(追正四至七,用户六轮裁定):产出改仓无关指导 SKILL build-release 落 evo-adr(第十 skill,ADR-0013、REQ-005),类型路由六型(原七型,fleet-heavy 按追正六剔除)七要素加公共契约节加可拷改模板四件;通用性终约束(追正七,最高优先):私有名与实证引零进 SKILL,契约全参数化(镜像域 mirror-host 形、安装管理器元数据抽象),实证留档仅此 diary:采守先例实录 = aria2 v1.37.2 十五轮自含静态与跨宿主容器闸、ark v1.3.0 Draft 态代发加零上传红灯教训、hst v2.0.0 双通道、browse v0.2.0 与 chromeInstall 双职责、reader 双通道互认五端验收、digest 三方对账;doc-gov 草稿篇 flow-build.md 退役并入,三面回退 9 篇;旧形态快核轮作废,定形另送新轮
- 模板家族定制(用户令):模板按本地各仓实际工作流与 git 历史定制,弃教科书空壳;实读四仓 workflows(ark build.yml、browse release.yml、hst dev-release.yml 加 aria2 release.yml 摘要)取实证形:env-remote r2: 配置形(RCLONE_CONFIG_R2 族加 NO_CHECK_BUCKET 必带)、三岗矩阵(linux 本职、win-gnu 交叉 mingw、mac arm64)、paths 过滤防 docs-only 空转、dev prerelease 滚动加正式 --latest 双通道、幂等挂件(view 加 create 加 upload clobber)、版本一致性闸 step、交叉件专岗冒烟、单顶层目录包形(bin 加 README 加 LICENSE,win zip 他 tar.gz,逐包边车)、段制三段(版本 immutable copy-only、stable sync delete-excluded、dev 随仓)、dispatch 补推带 tag 入参、零上传红灯清点;OfficeCLI publish-* 为上游形态未采
- 三段式终裁(用户令):标准正源 = 本地编译(主开发机交叉加实机矩阵)加 GitHub 产物发布(gh release 直发)加 CI/CD Action 自动播种 R2(release published 触发拉资产灌段);CI 面不编译,测试岗随仓裁;模板骨架重构为「本地编译与发布命令面 + r2-seed 播种 workflow」两件,通用 CI build 骨架退役
- 本地打包点明(用户令):舰队含 python 与 typescript 程序,故编译与打包都在本地(npm pack 与 python -m build 出离线包),GitHub 面只产物发布,Action 面只播种;三段式措辞统一「本地编译和打包」
- 终轮快核处置:F1 指针串、F2 白名单补 tgz 与 whl(半上传实质后果项)、F3 发布 glob 改 dist 全量、F4 三段式措辞残留四处(npm 与 py 单 job 措辞、版本闸与解包冒烟本地化、补推口形)、F5 同步面五处(CHANGELOG 两处、REQ-005、ADR-0013、根 README)全修;G1 红灯三段报数(stable 漏滚也红)、G2 manifest 补不适用行、G3 dev 段 if 注记,全采纳
- 终判补一:native 形「裸二进制」选项剔除(同包形契约,防绕白名单静默漏灌,评审同机制残留抓出)
- 挂账两条 G 记档:发布行 dist/* 若吃到解包冒烟残留目录会传 gh 报错(非静默,模板可加 dist 内容清理防呆);「红灯三段报数」措辞与实际清点两段(版本加 stable)口径差一(dev 选装才三段),下一批顺手正
- 总台追正(轻量一笔,CI 绿即收):补「上游追新」专节(common-contract 第八节,fork 维护仓仓无关配方:upstream merge 加冲突面纪律加版本 bump 加回跑矩阵加追新节奏;排障表补核心源冲突行;type-recipes native 形与 README 索引指针同步),aria2 回执 grep 追新零命中曝出

## 第七十六批:封版 v0.9.0 与三端铺开

- 封版面:CHANGELOG 转版(第七十五批入 [0.9.0] 含上游追新追正两笔)、四插件 0.2.0 升 0.3.0(八 manifest 加市场四条目加四 README 当前发布共十六处)加 pyproject 与 uv.lock 0.9.0、AGENTS 阶段行、README 钉版、ROADMAP 里程碑;tag v0.9.0 加 Release;三端 marketplace 加 plugin update 铺开 build-release
- 封版回执:快核 F1(残留暂无行)加 G1(追正归一笔)处置后 CONFIRM;推 main(ebee602..7417f6e 两笔)、tag v0.9.0、GitHub Release v0.9.0;CI success(7417f6e)
- 三端 skill 更新回执:claude 在位三端(wsl、lan-win、lan-mac)marketplace update 加四插件 update 0.2.0 到 0.3.0 全绿,evo-adr 0.3.0 缓存实证含 build-release(code-kit、doc-gov、gh-issue、build-release 四目录),旧 0.2.0 缓存三端清讫;lan-ubuntu 与 lan-linux 无 claude 不装

## 第七十七批:cli-docs 对外面双标准 skill 增编

- 总台立单,归口裁定:新立 skill cli-docs(照 build-release 形)落 evo-adr 第五(第十一 skill,ADR-0014、REQ-006),与 doc-gov(仓文档体系)和 code-kit tool-cli-agents(发现通道设计)分工互引
- 甲面研究:ripgrep、fzf、bat、fd 四 README 实读(WebFetch 原文),共性提炼(badges 三枚内、特性演示前置、全平台安装、配置样例、教程式使用、排障与许可尾节),四节主骨架定稿加写法纪律反面清单
- 乙面研究:hst fmtio.rs 输出契约实读(--format kv|json|jsonl 加 --json 简写互斥、{ok,data|error,meta} 信封、stderr 单行 JSON、preserve_order)加 omc output.ts 信封实读({ok,scope,data,hints} 六 format、filter/fields、hints 即 CTA、消息型回退),抽象为协议标准;--llms 正名与 120 行上限与活命令树渲染禁手维护(全派生或 curated 加守卫)入文
- 同步面:守卫十一 skill、pre-commit 第十一段、双 manifest 与市场描述、四处 README 与 gates 计数、tool-cli-agents 互引注、CHANGELOG、ROADMAP
- 总台细则补单(第二批复入第七十七批):乙面定为旗标七件加类型化 CTA(双语言参考 github.com/wevm/incur 的 TS 与 Rust 双实现);七件 = --filter-output 键路径(含数组索引)、--format toon/json/yaml/md、--full-output 全信封、--help/-h、--llms、--json 简写、--schema 三面 Schema;CTA 类型化 commands{command,args,description} ok 与 error 回执皆可携,人读形 Next: 块同源渲染;templates 加双语言实现模板(TS 加 Rust 各一份)
- 参考实现映射注记(留档不入 skill):omc 已有 --filter 与 --format 族(toon/text/json/yaml/md/jsonl)与 --llms 与 --json 与 hints(CTA 同概念未类型化),缺 --schema 与 --full-output 名形(其 --verbose 前置信封字段近义);hst 有 --format kv|json|jsonl 加 --json;两仓后续按七件对齐是各自仓的改造批
- 总台三号补单与裁定修正复入:默认帮助说明标准入 SKILL(底稿 S039 直读,参考实现 /tmp/incur 全源直读:Help.ts 加 cta.ts 加 Cli.ts);乙面四件定形(手册、旗标全家与类型化 CTA、默认帮助面、自省);CTA 实形 = 字符串或 command/args/options/description 折算(CLI 名自动前缀,args 值 true 折占位),渲染 Suggested commands: 描述行加两空格缩进,进信封 meta 人机同源;帮助节序定稿(头行 name@version、Usage synopsis、Arguments、Options 枚举全值默认后缀弃用前缀、Examples、Global Options 字典序、Environment Variables set 脱敏尾四);mcp 与 http api 用户裁为可选件(默认不做)独立附录章;快核 F1 至 F3(longDescription 计数、Unreleased 残留暂无、REQ 与 ADR 口径)与 G1 至 G3(Rust 补 error 与 render、失败形补 cta、error 字段定形)并轮处置
- 第七十七批收束:四轮快核(增编轮 F1 至 F3 加 G1 至 G3、三号补单轮、二轮口径、三轮八处刷四件)全闭 CONFIRM;推 main 五笔,CI success;总台回执附 skill 路径与甲乙两面要点映射
