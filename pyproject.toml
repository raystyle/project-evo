[project]
name = "project-evo"
version = "0.7.0"
description = "项目进化:插件市场仓(本文件仅为仓内维护环境;skill 与脚本随 plugins/ 四插件分发)"
requires-python = ">=3.12"
dependencies = []
license = { text = "MIT" }
authors = [{ name = "ray" }]
keywords = ["agent", "skill", "plugin", "marketplace", "documentation"]

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
